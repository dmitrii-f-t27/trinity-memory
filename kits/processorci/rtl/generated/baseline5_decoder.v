// ============================================================================
// Generated from t27 spec: TrinityBaseline5DecoderT27
// DO NOT EDIT - generated by t27c gen-verilog
// phi^2 + 1/phi^2 = 3 | TRINITY
// ============================================================================

`timescale 1ns / 1ps
`default_nettype none

module TrinityBaseline5DecoderT27 (
    input  wire        clk,
    input  wire        rst_n,
    input  wire        en,
    input  wire [15:0] code,
    output wire        ready,
    output wire [15:0] result
);

    // -------------------------------------------------------
    // R-SI-1: multiplication helper (no `*` operator)
    // -------------------------------------------------------
    function [63:0] __mul_noop; // t27#1886: 64-bit, u64 products no longer truncate
        input [63:0] a;
        input [63:0] b;
        integer i;
        reg [127:0] acc;
        begin
            acc = 128'd0;
            for (i = 0; i < 64; i = i + 1) begin
                if (b[i]) acc = acc + ({64'd0, a} << i);
            end
            __mul_noop = acc[63:0];
        end
    endfunction

    assign ready = 1'b1;

    // -------------------------------------------------------
    // Combinational logic (from function declarations)
    // -------------------------------------------------------

    // function: on_comb
    function [15:0] on_comb; // -> u16
        input [15:0] code;
        begin : on_comb_body
            reg __t27_ret;
            __t27_ret = 1'b0;
            if ((((((($signed({1'b0, code}) < $signed(1024)) && ($signed((code & 3)) < $signed(3))) && ($signed(((code >> 2) & 3)) < $signed(3))) && ($signed(((code >> 4) & 3)) < $signed(3))) && ($signed(((code >> 6) & 3)) < $signed(3))) && ($signed(((code >> 8) & 3)) < $signed(3)))) begin
                on_comb = (1024 | code);
                __t27_ret = 1'b1;
            end else begin
                on_comb = 0;
                __t27_ret = 1'b1;
            end
        end
    endfunction

    assign result = on_comb(code);
    // -------------------------------------------------------
    // NOT LOWERED BY THIS BACKEND
    //
    // `gen-verilog` emits synthesizable RTL. A test, an invariant and a
    // bench are simulation constructs -- yosys turns each `$display` into a
    // `$print` cell and nextpnr has no BEL to place one. They are carried by
    // `gen-verilog-for-simulation`, which lowers every declaration below.
    //
    // test      logical_zero
    // test      negative_five
    // test      positive_five
    // test      reserved_lane
    // test      reserved_tail
    // test      outside_word
    // -------------------------------------------------------

endmodule

`default_nettype wire
