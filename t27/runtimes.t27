// SPDX-License-Identifier: Apache-2.0
// GENERATED by tools/generate-runtime-tables.py from specs/runtimes/*.json; do not edit.
// Runtime tables (issue #51): for each pinned runtime, the ggml type ids it defines
// (block size in weights, bytes per block, the ggml_nbytes rule) and the architecture
// names its model loader knows. Sources, per runtime:
//   1 llama.cpp: ggml-org/llama.cpp@e6ab7c1a41054a888ada952eab4c886444c2f5ad
//   2 PrismML llama.cpp fork: PrismML-Eng/llama.cpp@bdc23b56b4458b9f1655aec5287f3ab56ee8daaa
//   3 bitnet.cpp: isHuangXin/llama.cpp@390c307752ab78fd8189f359d6954c9ba1be74af
// Their files and blob SHAs are listed in the specs; the rules they restate are cited
// in t27/live.t27, the module that reads these tables.
module TrinityMemoryRuntimes {
    const TRT_LLAMA_CPP: i32 = 1;
    const TRT_PRISMML: i32 = 2;
    const TRT_BITNET_CPP: i32 = 3;
    const TRT_RULE_BLOCKS: i32 = 0;  // ne0 / block * bytes, times the other dims
    const TRT_RULE_I2S: i32 = 1;     // bitnet.cpp I2_S and TL1: elements / 4 + 32
    const TRT_RULE_TL2: i32 = 2;     // bitnet.cpp TL2, ggml_nbytes special case

    // GGML_TYPE_COUNT: ids at or above it are refused by gguf.cpp.
    fn trt_type_count(runtime: i32) -> u32 {
        if (runtime == 1) { return 43; }
        if (runtime == 2) { return 144; }
        if (runtime == 3) { return 43; }
        return 0;
    }

    // Weights per block; 0 when the runtime defines no traits for the id.
    fn trt_blck(runtime: i32, type_id: u32) -> u64 {
        if (runtime == 1) {
            if (type_id == 0) { return 1; }  // f32
            if (type_id == 1) { return 1; }  // f16
            if (type_id == 2) { return 32; }  // q4_0
            if (type_id == 3) { return 32; }  // q4_1
            if (type_id == 6) { return 32; }  // q5_0
            if (type_id == 7) { return 32; }  // q5_1
            if (type_id == 8) { return 32; }  // q8_0
            if (type_id == 9) { return 32; }  // q8_1
            if (type_id == 10) { return 256; }  // q2_K
            if (type_id == 11) { return 256; }  // q3_K
            if (type_id == 12) { return 256; }  // q4_K
            if (type_id == 13) { return 256; }  // q5_K
            if (type_id == 14) { return 256; }  // q6_K
            if (type_id == 15) { return 256; }  // q8_K
            if (type_id == 16) { return 256; }  // iq2_xxs
            if (type_id == 17) { return 256; }  // iq2_xs
            if (type_id == 18) { return 256; }  // iq3_xxs
            if (type_id == 19) { return 256; }  // iq1_s
            if (type_id == 20) { return 32; }  // iq4_nl
            if (type_id == 21) { return 256; }  // iq3_s
            if (type_id == 22) { return 256; }  // iq2_s
            if (type_id == 23) { return 256; }  // iq4_xs
            if (type_id == 24) { return 1; }  // i8
            if (type_id == 25) { return 1; }  // i16
            if (type_id == 26) { return 1; }  // i32
            if (type_id == 27) { return 1; }  // i64
            if (type_id == 28) { return 1; }  // f64
            if (type_id == 29) { return 256; }  // iq1_m
            if (type_id == 30) { return 1; }  // bf16
            if (type_id == 34) { return 256; }  // tq1_0
            if (type_id == 35) { return 256; }  // tq2_0
            if (type_id == 39) { return 32; }  // mxfp4
            if (type_id == 40) { return 64; }  // nvfp4
            if (type_id == 41) { return 128; }  // q1_0
            if (type_id == 42) { return 64; }  // q2_0
            return 0;
        }
        if (runtime == 2) {
            if (type_id == 0) { return 1; }  // f32
            if (type_id == 1) { return 1; }  // f16
            if (type_id == 2) { return 32; }  // q4_0
            if (type_id == 3) { return 32; }  // q4_1
            if (type_id == 6) { return 32; }  // q5_0
            if (type_id == 7) { return 32; }  // q5_1
            if (type_id == 8) { return 32; }  // q8_0
            if (type_id == 9) { return 32; }  // q8_1
            if (type_id == 10) { return 256; }  // q2_K
            if (type_id == 11) { return 256; }  // q3_K
            if (type_id == 12) { return 256; }  // q4_K
            if (type_id == 13) { return 256; }  // q5_K
            if (type_id == 14) { return 256; }  // q6_K
            if (type_id == 15) { return 256; }  // q8_K
            if (type_id == 16) { return 256; }  // iq2_xxs
            if (type_id == 17) { return 256; }  // iq2_xs
            if (type_id == 18) { return 256; }  // iq3_xxs
            if (type_id == 19) { return 256; }  // iq1_s
            if (type_id == 20) { return 32; }  // iq4_nl
            if (type_id == 21) { return 256; }  // iq3_s
            if (type_id == 22) { return 256; }  // iq2_s
            if (type_id == 23) { return 256; }  // iq4_xs
            if (type_id == 24) { return 1; }  // i8
            if (type_id == 25) { return 1; }  // i16
            if (type_id == 26) { return 1; }  // i32
            if (type_id == 27) { return 1; }  // i64
            if (type_id == 28) { return 1; }  // f64
            if (type_id == 29) { return 256; }  // iq1_m
            if (type_id == 30) { return 1; }  // bf16
            if (type_id == 34) { return 256; }  // tq1_0
            if (type_id == 35) { return 256; }  // tq2_0
            if (type_id == 39) { return 32; }  // mxfp4
            if (type_id == 40) { return 64; }  // nvfp4
            if (type_id == 41) { return 128; }  // q1_0
            if (type_id == 42) { return 64; }  // q2_0
            if (type_id == 142) { return 128; }  // pq2_0
            if (type_id == 143) { return 128; }  // ptq1_0
            return 0;
        }
        if (runtime == 3) {
            if (type_id == 0) { return 1; }  // f32
            if (type_id == 1) { return 1; }  // f16
            if (type_id == 2) { return 32; }  // q4_0
            if (type_id == 3) { return 32; }  // q4_1
            if (type_id == 6) { return 32; }  // q5_0
            if (type_id == 7) { return 32; }  // q5_1
            if (type_id == 8) { return 32; }  // q8_0
            if (type_id == 9) { return 32; }  // q8_1
            if (type_id == 10) { return 256; }  // q2_K
            if (type_id == 11) { return 256; }  // q3_K
            if (type_id == 12) { return 256; }  // q4_K
            if (type_id == 13) { return 256; }  // q5_K
            if (type_id == 14) { return 256; }  // q6_K
            if (type_id == 15) { return 256; }  // q8_K
            if (type_id == 16) { return 256; }  // iq2_xxs
            if (type_id == 17) { return 256; }  // iq2_xs
            if (type_id == 18) { return 256; }  // iq3_xxs
            if (type_id == 19) { return 256; }  // iq1_s
            if (type_id == 20) { return 32; }  // iq4_nl
            if (type_id == 21) { return 256; }  // iq3_s
            if (type_id == 22) { return 256; }  // iq2_s
            if (type_id == 23) { return 256; }  // iq4_xs
            if (type_id == 24) { return 1; }  // i8
            if (type_id == 25) { return 1; }  // i16
            if (type_id == 26) { return 1; }  // i32
            if (type_id == 27) { return 1; }  // i64
            if (type_id == 28) { return 1; }  // f64
            if (type_id == 29) { return 256; }  // iq1_m
            if (type_id == 30) { return 1; }  // bf16
            if (type_id == 34) { return 256; }  // tq1_0
            if (type_id == 35) { return 256; }  // tq2_0
            if (type_id == 36) { return 1; }  // i2_s
            if (type_id == 37) { return 1; }  // i8_s
            if (type_id == 38) { return 1; }  // tl1
            if (type_id == 39) { return 32; }  // mxfp4
            if (type_id == 40) { return 64; }  // nvfp4
            if (type_id == 41) { return 128; }  // q1_0
            if (type_id == 42) { return 1; }  // tl2
            return 0;
        }
        return 0;
    }

    // Bytes per block; 0 when the runtime defines no traits for the id.
    fn trt_bytes(runtime: i32, type_id: u32) -> u64 {
        if (runtime == 1) {
            if (type_id == 0) { return 4; }  // f32
            if (type_id == 1) { return 2; }  // f16
            if (type_id == 2) { return 18; }  // q4_0
            if (type_id == 3) { return 20; }  // q4_1
            if (type_id == 6) { return 22; }  // q5_0
            if (type_id == 7) { return 24; }  // q5_1
            if (type_id == 8) { return 34; }  // q8_0
            if (type_id == 9) { return 36; }  // q8_1
            if (type_id == 10) { return 84; }  // q2_K
            if (type_id == 11) { return 110; }  // q3_K
            if (type_id == 12) { return 144; }  // q4_K
            if (type_id == 13) { return 176; }  // q5_K
            if (type_id == 14) { return 210; }  // q6_K
            if (type_id == 15) { return 292; }  // q8_K
            if (type_id == 16) { return 66; }  // iq2_xxs
            if (type_id == 17) { return 74; }  // iq2_xs
            if (type_id == 18) { return 98; }  // iq3_xxs
            if (type_id == 19) { return 50; }  // iq1_s
            if (type_id == 20) { return 18; }  // iq4_nl
            if (type_id == 21) { return 110; }  // iq3_s
            if (type_id == 22) { return 82; }  // iq2_s
            if (type_id == 23) { return 136; }  // iq4_xs
            if (type_id == 24) { return 1; }  // i8
            if (type_id == 25) { return 2; }  // i16
            if (type_id == 26) { return 4; }  // i32
            if (type_id == 27) { return 8; }  // i64
            if (type_id == 28) { return 8; }  // f64
            if (type_id == 29) { return 56; }  // iq1_m
            if (type_id == 30) { return 2; }  // bf16
            if (type_id == 34) { return 54; }  // tq1_0
            if (type_id == 35) { return 66; }  // tq2_0
            if (type_id == 39) { return 17; }  // mxfp4
            if (type_id == 40) { return 36; }  // nvfp4
            if (type_id == 41) { return 18; }  // q1_0
            if (type_id == 42) { return 18; }  // q2_0
            return 0;
        }
        if (runtime == 2) {
            if (type_id == 0) { return 4; }  // f32
            if (type_id == 1) { return 2; }  // f16
            if (type_id == 2) { return 18; }  // q4_0
            if (type_id == 3) { return 20; }  // q4_1
            if (type_id == 6) { return 22; }  // q5_0
            if (type_id == 7) { return 24; }  // q5_1
            if (type_id == 8) { return 34; }  // q8_0
            if (type_id == 9) { return 36; }  // q8_1
            if (type_id == 10) { return 84; }  // q2_K
            if (type_id == 11) { return 110; }  // q3_K
            if (type_id == 12) { return 144; }  // q4_K
            if (type_id == 13) { return 176; }  // q5_K
            if (type_id == 14) { return 210; }  // q6_K
            if (type_id == 15) { return 292; }  // q8_K
            if (type_id == 16) { return 66; }  // iq2_xxs
            if (type_id == 17) { return 74; }  // iq2_xs
            if (type_id == 18) { return 98; }  // iq3_xxs
            if (type_id == 19) { return 50; }  // iq1_s
            if (type_id == 20) { return 18; }  // iq4_nl
            if (type_id == 21) { return 110; }  // iq3_s
            if (type_id == 22) { return 82; }  // iq2_s
            if (type_id == 23) { return 136; }  // iq4_xs
            if (type_id == 24) { return 1; }  // i8
            if (type_id == 25) { return 2; }  // i16
            if (type_id == 26) { return 4; }  // i32
            if (type_id == 27) { return 8; }  // i64
            if (type_id == 28) { return 8; }  // f64
            if (type_id == 29) { return 56; }  // iq1_m
            if (type_id == 30) { return 2; }  // bf16
            if (type_id == 34) { return 54; }  // tq1_0
            if (type_id == 35) { return 66; }  // tq2_0
            if (type_id == 39) { return 17; }  // mxfp4
            if (type_id == 40) { return 36; }  // nvfp4
            if (type_id == 41) { return 18; }  // q1_0
            if (type_id == 42) { return 18; }  // q2_0
            if (type_id == 142) { return 34; }  // pq2_0
            if (type_id == 143) { return 28; }  // ptq1_0
            return 0;
        }
        if (runtime == 3) {
            if (type_id == 0) { return 4; }  // f32
            if (type_id == 1) { return 2; }  // f16
            if (type_id == 2) { return 18; }  // q4_0
            if (type_id == 3) { return 20; }  // q4_1
            if (type_id == 6) { return 22; }  // q5_0
            if (type_id == 7) { return 24; }  // q5_1
            if (type_id == 8) { return 34; }  // q8_0
            if (type_id == 9) { return 36; }  // q8_1
            if (type_id == 10) { return 84; }  // q2_K
            if (type_id == 11) { return 110; }  // q3_K
            if (type_id == 12) { return 144; }  // q4_K
            if (type_id == 13) { return 176; }  // q5_K
            if (type_id == 14) { return 210; }  // q6_K
            if (type_id == 15) { return 292; }  // q8_K
            if (type_id == 16) { return 66; }  // iq2_xxs
            if (type_id == 17) { return 74; }  // iq2_xs
            if (type_id == 18) { return 98; }  // iq3_xxs
            if (type_id == 19) { return 50; }  // iq1_s
            if (type_id == 20) { return 18; }  // iq4_nl
            if (type_id == 21) { return 110; }  // iq3_s
            if (type_id == 22) { return 82; }  // iq2_s
            if (type_id == 23) { return 136; }  // iq4_xs
            if (type_id == 24) { return 1; }  // i8
            if (type_id == 25) { return 2; }  // i16
            if (type_id == 26) { return 4; }  // i32
            if (type_id == 27) { return 8; }  // i64
            if (type_id == 28) { return 8; }  // f64
            if (type_id == 29) { return 56; }  // iq1_m
            if (type_id == 30) { return 2; }  // bf16
            if (type_id == 34) { return 54; }  // tq1_0
            if (type_id == 35) { return 66; }  // tq2_0
            if (type_id == 36) { return 1; }  // i2_s
            if (type_id == 37) { return 1; }  // i8_s
            if (type_id == 38) { return 1; }  // tl1
            if (type_id == 39) { return 17; }  // mxfp4
            if (type_id == 40) { return 36; }  // nvfp4
            if (type_id == 41) { return 18; }  // q1_0
            if (type_id == 42) { return 1; }  // tl2
            return 0;
        }
        return 0;
    }

    // The ggml_nbytes rule of the id (TRT_RULE_*).
    fn trt_rule(runtime: i32, type_id: u32) -> i32 {
        if (runtime == 3) {
            if (type_id == 36) { return TRT_RULE_I2S; }  // i2_s
            if (type_id == 38) { return TRT_RULE_I2S; }  // tl1
            if (type_id == 42) { return TRT_RULE_TL2; }  // tl2
        }
        return TRT_RULE_BLOCKS;
    }

    // Whether the runtime's model loader applies prism.hadamard.* (only the PrismML fork).
    fn trt_hadamard(runtime: i32) -> bool {
        if (runtime == 1) { return false; }
        if (runtime == 2) { return true; }
        if (runtime == 3) { return false; }
        return false;
    }

    // Whether data[at .. at + size) is an architecture name the runtime's loader knows
    // (LLM_ARCH_NAMES; the caller passes the name up to its first NUL, as the loader reads it).
    fn trt_arch_known(runtime: i32, data: []u8, at: usize, size: usize) -> bool {
        if (runtime == 1) {
            if (tf_same_text(data, at, size, "clip", 4)) { return true; }
            if (tf_same_text(data, at, size, "llama", 5)) { return true; }
            if (tf_same_text(data, at, size, "llama4", 6)) { return true; }
            if (tf_same_text(data, at, size, "deci", 4)) { return true; }
            if (tf_same_text(data, at, size, "falcon", 6)) { return true; }
            if (tf_same_text(data, at, size, "grok", 4)) { return true; }
            if (tf_same_text(data, at, size, "gpt2", 4)) { return true; }
            if (tf_same_text(data, at, size, "gptj", 4)) { return true; }
            if (tf_same_text(data, at, size, "gptneox", 7)) { return true; }
            if (tf_same_text(data, at, size, "mpt", 3)) { return true; }
            if (tf_same_text(data, at, size, "baichuan", 8)) { return true; }
            if (tf_same_text(data, at, size, "starcoder", 9)) { return true; }
            if (tf_same_text(data, at, size, "refact", 6)) { return true; }
            if (tf_same_text(data, at, size, "bert", 4)) { return true; }
            if (tf_same_text(data, at, size, "modern-bert", 11)) { return true; }
            if (tf_same_text(data, at, size, "nomic-bert", 10)) { return true; }
            if (tf_same_text(data, at, size, "nomic-bert-moe", 14)) { return true; }
            if (tf_same_text(data, at, size, "neo-bert", 8)) { return true; }
            if (tf_same_text(data, at, size, "jina-bert-v2", 12)) { return true; }
            if (tf_same_text(data, at, size, "jina-bert-v3", 12)) { return true; }
            if (tf_same_text(data, at, size, "eurobert", 8)) { return true; }
            if (tf_same_text(data, at, size, "bloom", 5)) { return true; }
            if (tf_same_text(data, at, size, "stablelm", 8)) { return true; }
            if (tf_same_text(data, at, size, "qwen", 4)) { return true; }
            if (tf_same_text(data, at, size, "qwen2", 5)) { return true; }
            if (tf_same_text(data, at, size, "qwen2moe", 8)) { return true; }
            if (tf_same_text(data, at, size, "qwen2vl", 7)) { return true; }
            if (tf_same_text(data, at, size, "qwen3", 5)) { return true; }
            if (tf_same_text(data, at, size, "qwen3moe", 8)) { return true; }
            if (tf_same_text(data, at, size, "qwen3next", 9)) { return true; }
            if (tf_same_text(data, at, size, "qwen3vl", 7)) { return true; }
            if (tf_same_text(data, at, size, "qwen3vlmoe", 10)) { return true; }
            if (tf_same_text(data, at, size, "qwen35", 6)) { return true; }
            if (tf_same_text(data, at, size, "qwen35moe", 9)) { return true; }
            if (tf_same_text(data, at, size, "qwen4exp", 8)) { return true; }
            if (tf_same_text(data, at, size, "phi2", 4)) { return true; }
            if (tf_same_text(data, at, size, "phi3", 4)) { return true; }
            if (tf_same_text(data, at, size, "phimoe", 6)) { return true; }
            if (tf_same_text(data, at, size, "plamo", 5)) { return true; }
            if (tf_same_text(data, at, size, "plamo2", 6)) { return true; }
            if (tf_same_text(data, at, size, "plamo3", 6)) { return true; }
            if (tf_same_text(data, at, size, "codeshell", 9)) { return true; }
            if (tf_same_text(data, at, size, "orion", 5)) { return true; }
            if (tf_same_text(data, at, size, "internlm2", 9)) { return true; }
            if (tf_same_text(data, at, size, "minicpm", 7)) { return true; }
            if (tf_same_text(data, at, size, "minicpm3", 8)) { return true; }
            if (tf_same_text(data, at, size, "gemma", 5)) { return true; }
            if (tf_same_text(data, at, size, "gemma2", 6)) { return true; }
            if (tf_same_text(data, at, size, "gemma3", 6)) { return true; }
            if (tf_same_text(data, at, size, "gemma3n", 7)) { return true; }
            if (tf_same_text(data, at, size, "gemma4", 6)) { return true; }
            if (tf_same_text(data, at, size, "gemma4-assistant", 16)) { return true; }
            if (tf_same_text(data, at, size, "gemma-embedding", 15)) { return true; }
            if (tf_same_text(data, at, size, "starcoder2", 10)) { return true; }
            if (tf_same_text(data, at, size, "mamba", 5)) { return true; }
            if (tf_same_text(data, at, size, "mamba2", 6)) { return true; }
            if (tf_same_text(data, at, size, "maple", 5)) { return true; }
            if (tf_same_text(data, at, size, "jamba", 5)) { return true; }
            if (tf_same_text(data, at, size, "falcon-h1", 9)) { return true; }
            if (tf_same_text(data, at, size, "xverse", 6)) { return true; }
            if (tf_same_text(data, at, size, "command-r", 9)) { return true; }
            if (tf_same_text(data, at, size, "cohere2", 7)) { return true; }
            if (tf_same_text(data, at, size, "cohere2moe", 10)) { return true; }
            if (tf_same_text(data, at, size, "dbrx", 4)) { return true; }
            if (tf_same_text(data, at, size, "olmo", 4)) { return true; }
            if (tf_same_text(data, at, size, "olmo2", 5)) { return true; }
            if (tf_same_text(data, at, size, "olmoe", 5)) { return true; }
            if (tf_same_text(data, at, size, "muse-glimmer", 12)) { return true; }
            if (tf_same_text(data, at, size, "openelm", 7)) { return true; }
            if (tf_same_text(data, at, size, "arctic", 6)) { return true; }
            if (tf_same_text(data, at, size, "deepseek", 8)) { return true; }
            if (tf_same_text(data, at, size, "deepseek2", 9)) { return true; }
            if (tf_same_text(data, at, size, "deepseek2-ocr", 13)) { return true; }
            if (tf_same_text(data, at, size, "deepseek32", 10)) { return true; }
            if (tf_same_text(data, at, size, "deepseek4", 9)) { return true; }
            if (tf_same_text(data, at, size, "chatglm", 7)) { return true; }
            if (tf_same_text(data, at, size, "glm4", 4)) { return true; }
            if (tf_same_text(data, at, size, "glm4moe", 7)) { return true; }
            if (tf_same_text(data, at, size, "glm-dsa", 7)) { return true; }
            if (tf_same_text(data, at, size, "bitnet", 6)) { return true; }
            if (tf_same_text(data, at, size, "t5", 2)) { return true; }
            if (tf_same_text(data, at, size, "t5encoder", 9)) { return true; }
            if (tf_same_text(data, at, size, "jais", 4)) { return true; }
            if (tf_same_text(data, at, size, "jais2", 5)) { return true; }
            if (tf_same_text(data, at, size, "nemotron", 8)) { return true; }
            if (tf_same_text(data, at, size, "nemotron_h", 10)) { return true; }
            if (tf_same_text(data, at, size, "nemotron_h_moe", 14)) { return true; }
            if (tf_same_text(data, at, size, "exaone", 6)) { return true; }
            if (tf_same_text(data, at, size, "exaone4", 7)) { return true; }
            if (tf_same_text(data, at, size, "exaone-moe", 10)) { return true; }
            if (tf_same_text(data, at, size, "rwkv6", 5)) { return true; }
            if (tf_same_text(data, at, size, "rwkv6qwen2", 10)) { return true; }
            if (tf_same_text(data, at, size, "rwkv7", 5)) { return true; }
            if (tf_same_text(data, at, size, "arwkv7", 6)) { return true; }
            if (tf_same_text(data, at, size, "granite", 7)) { return true; }
            if (tf_same_text(data, at, size, "granitemoe", 10)) { return true; }
            if (tf_same_text(data, at, size, "granitehybrid", 13)) { return true; }
            if (tf_same_text(data, at, size, "graniteswitch", 13)) { return true; }
            if (tf_same_text(data, at, size, "granite_swa", 11)) { return true; }
            if (tf_same_text(data, at, size, "chameleon", 9)) { return true; }
            if (tf_same_text(data, at, size, "wavtokenizer-dec", 16)) { return true; }
            if (tf_same_text(data, at, size, "plm", 3)) { return true; }
            if (tf_same_text(data, at, size, "bailingmoe", 10)) { return true; }
            if (tf_same_text(data, at, size, "bailingmoe2", 11)) { return true; }
            if (tf_same_text(data, at, size, "bailingmoe3", 11)) { return true; }
            if (tf_same_text(data, at, size, "dots1", 5)) { return true; }
            if (tf_same_text(data, at, size, "dots3note", 9)) { return true; }
            if (tf_same_text(data, at, size, "arcee", 5)) { return true; }
            if (tf_same_text(data, at, size, "afmoe", 5)) { return true; }
            if (tf_same_text(data, at, size, "laguna", 6)) { return true; }
            if (tf_same_text(data, at, size, "ernie4_5", 8)) { return true; }
            if (tf_same_text(data, at, size, "ernie4_5-moe", 12)) { return true; }
            if (tf_same_text(data, at, size, "hunyuan-moe", 11)) { return true; }
            if (tf_same_text(data, at, size, "hunyuan-dense", 13)) { return true; }
            if (tf_same_text(data, at, size, "hunyuan_vl", 10)) { return true; }
            if (tf_same_text(data, at, size, "hy_v3", 5)) { return true; }
            if (tf_same_text(data, at, size, "hy_v4", 5)) { return true; }
            if (tf_same_text(data, at, size, "smollm3", 7)) { return true; }
            if (tf_same_text(data, at, size, "gpt-oss", 7)) { return true; }
            if (tf_same_text(data, at, size, "lfm2", 4)) { return true; }
            if (tf_same_text(data, at, size, "lfm2moe", 7)) { return true; }
            if (tf_same_text(data, at, size, "dream", 5)) { return true; }
            if (tf_same_text(data, at, size, "smallthinker", 12)) { return true; }
            if (tf_same_text(data, at, size, "llada", 5)) { return true; }
            if (tf_same_text(data, at, size, "llada-moe", 9)) { return true; }
            if (tf_same_text(data, at, size, "seed_oss", 8)) { return true; }
            if (tf_same_text(data, at, size, "grovemoe", 8)) { return true; }
            if (tf_same_text(data, at, size, "apertus", 7)) { return true; }
            if (tf_same_text(data, at, size, "minimax-01", 10)) { return true; }
            if (tf_same_text(data, at, size, "hrm_text", 8)) { return true; }
            if (tf_same_text(data, at, size, "minimax-m2", 10)) { return true; }
            if (tf_same_text(data, at, size, "minimax-m3", 10)) { return true; }
            if (tf_same_text(data, at, size, "cogvlm", 6)) { return true; }
            if (tf_same_text(data, at, size, "rnd1", 4)) { return true; }
            if (tf_same_text(data, at, size, "pangu-embedded", 14)) { return true; }
            if (tf_same_text(data, at, size, "mistral3", 8)) { return true; }
            if (tf_same_text(data, at, size, "eagle3", 6)) { return true; }
            if (tf_same_text(data, at, size, "dflash", 6)) { return true; }
            if (tf_same_text(data, at, size, "mistral4", 8)) { return true; }
            if (tf_same_text(data, at, size, "paddleocr", 9)) { return true; }
            if (tf_same_text(data, at, size, "mimo2", 5)) { return true; }
            if (tf_same_text(data, at, size, "step35", 6)) { return true; }
            if (tf_same_text(data, at, size, "spark2_5", 8)) { return true; }
            if (tf_same_text(data, at, size, "llama-embed", 11)) { return true; }
            if (tf_same_text(data, at, size, "maincoder", 9)) { return true; }
            if (tf_same_text(data, at, size, "kimi-linear", 11)) { return true; }
            if (tf_same_text(data, at, size, "kimi-k3", 7)) { return true; }
            if (tf_same_text(data, at, size, "talkie", 6)) { return true; }
            if (tf_same_text(data, at, size, "mellum", 6)) { return true; }
            if (tf_same_text(data, at, size, "nanbeige", 8)) { return true; }
            if (tf_same_text(data, at, size, "qwen3tts", 8)) { return true; }
            if (tf_same_text(data, at, size, "pockettts", 9)) { return true; }
            if (tf_same_text(data, at, size, "(unknown)", 9)) { return true; }
            return false;
        }
        if (runtime == 2) {
            if (tf_same_text(data, at, size, "clip", 4)) { return true; }
            if (tf_same_text(data, at, size, "llama", 5)) { return true; }
            if (tf_same_text(data, at, size, "llama4", 6)) { return true; }
            if (tf_same_text(data, at, size, "deci", 4)) { return true; }
            if (tf_same_text(data, at, size, "falcon", 6)) { return true; }
            if (tf_same_text(data, at, size, "grok", 4)) { return true; }
            if (tf_same_text(data, at, size, "gpt2", 4)) { return true; }
            if (tf_same_text(data, at, size, "gptj", 4)) { return true; }
            if (tf_same_text(data, at, size, "gptneox", 7)) { return true; }
            if (tf_same_text(data, at, size, "mpt", 3)) { return true; }
            if (tf_same_text(data, at, size, "baichuan", 8)) { return true; }
            if (tf_same_text(data, at, size, "starcoder", 9)) { return true; }
            if (tf_same_text(data, at, size, "refact", 6)) { return true; }
            if (tf_same_text(data, at, size, "bert", 4)) { return true; }
            if (tf_same_text(data, at, size, "modern-bert", 11)) { return true; }
            if (tf_same_text(data, at, size, "nomic-bert", 10)) { return true; }
            if (tf_same_text(data, at, size, "nomic-bert-moe", 14)) { return true; }
            if (tf_same_text(data, at, size, "neo-bert", 8)) { return true; }
            if (tf_same_text(data, at, size, "jina-bert-v2", 12)) { return true; }
            if (tf_same_text(data, at, size, "jina-bert-v3", 12)) { return true; }
            if (tf_same_text(data, at, size, "eurobert", 8)) { return true; }
            if (tf_same_text(data, at, size, "bloom", 5)) { return true; }
            if (tf_same_text(data, at, size, "stablelm", 8)) { return true; }
            if (tf_same_text(data, at, size, "qwen", 4)) { return true; }
            if (tf_same_text(data, at, size, "qwen2", 5)) { return true; }
            if (tf_same_text(data, at, size, "qwen2moe", 8)) { return true; }
            if (tf_same_text(data, at, size, "qwen2vl", 7)) { return true; }
            if (tf_same_text(data, at, size, "qwen3", 5)) { return true; }
            if (tf_same_text(data, at, size, "qwen3moe", 8)) { return true; }
            if (tf_same_text(data, at, size, "qwen3next", 9)) { return true; }
            if (tf_same_text(data, at, size, "qwen3vl", 7)) { return true; }
            if (tf_same_text(data, at, size, "qwen3vlmoe", 10)) { return true; }
            if (tf_same_text(data, at, size, "qwen35", 6)) { return true; }
            if (tf_same_text(data, at, size, "qwen35moe", 9)) { return true; }
            if (tf_same_text(data, at, size, "dspark", 6)) { return true; }
            if (tf_same_text(data, at, size, "phi2", 4)) { return true; }
            if (tf_same_text(data, at, size, "phi3", 4)) { return true; }
            if (tf_same_text(data, at, size, "phimoe", 6)) { return true; }
            if (tf_same_text(data, at, size, "plamo", 5)) { return true; }
            if (tf_same_text(data, at, size, "plamo2", 6)) { return true; }
            if (tf_same_text(data, at, size, "plamo3", 6)) { return true; }
            if (tf_same_text(data, at, size, "codeshell", 9)) { return true; }
            if (tf_same_text(data, at, size, "orion", 5)) { return true; }
            if (tf_same_text(data, at, size, "internlm2", 9)) { return true; }
            if (tf_same_text(data, at, size, "minicpm", 7)) { return true; }
            if (tf_same_text(data, at, size, "minicpm3", 8)) { return true; }
            if (tf_same_text(data, at, size, "gemma", 5)) { return true; }
            if (tf_same_text(data, at, size, "gemma2", 6)) { return true; }
            if (tf_same_text(data, at, size, "gemma3", 6)) { return true; }
            if (tf_same_text(data, at, size, "gemma3n", 7)) { return true; }
            if (tf_same_text(data, at, size, "gemma4", 6)) { return true; }
            if (tf_same_text(data, at, size, "gemma4-assistant", 16)) { return true; }
            if (tf_same_text(data, at, size, "gemma-embedding", 15)) { return true; }
            if (tf_same_text(data, at, size, "starcoder2", 10)) { return true; }
            if (tf_same_text(data, at, size, "mamba", 5)) { return true; }
            if (tf_same_text(data, at, size, "mamba2", 6)) { return true; }
            if (tf_same_text(data, at, size, "jamba", 5)) { return true; }
            if (tf_same_text(data, at, size, "falcon-h1", 9)) { return true; }
            if (tf_same_text(data, at, size, "xverse", 6)) { return true; }
            if (tf_same_text(data, at, size, "command-r", 9)) { return true; }
            if (tf_same_text(data, at, size, "cohere2", 7)) { return true; }
            if (tf_same_text(data, at, size, "cohere2moe", 10)) { return true; }
            if (tf_same_text(data, at, size, "dbrx", 4)) { return true; }
            if (tf_same_text(data, at, size, "olmo", 4)) { return true; }
            if (tf_same_text(data, at, size, "olmo2", 5)) { return true; }
            if (tf_same_text(data, at, size, "olmoe", 5)) { return true; }
            if (tf_same_text(data, at, size, "muse-glimmer", 12)) { return true; }
            if (tf_same_text(data, at, size, "openelm", 7)) { return true; }
            if (tf_same_text(data, at, size, "arctic", 6)) { return true; }
            if (tf_same_text(data, at, size, "deepseek", 8)) { return true; }
            if (tf_same_text(data, at, size, "deepseek2", 9)) { return true; }
            if (tf_same_text(data, at, size, "deepseek2-ocr", 13)) { return true; }
            if (tf_same_text(data, at, size, "deepseek32", 10)) { return true; }
            if (tf_same_text(data, at, size, "deepseek4", 9)) { return true; }
            if (tf_same_text(data, at, size, "chatglm", 7)) { return true; }
            if (tf_same_text(data, at, size, "glm4", 4)) { return true; }
            if (tf_same_text(data, at, size, "glm4moe", 7)) { return true; }
            if (tf_same_text(data, at, size, "glm-dsa", 7)) { return true; }
            if (tf_same_text(data, at, size, "bitnet", 6)) { return true; }
            if (tf_same_text(data, at, size, "t5", 2)) { return true; }
            if (tf_same_text(data, at, size, "t5encoder", 9)) { return true; }
            if (tf_same_text(data, at, size, "jais", 4)) { return true; }
            if (tf_same_text(data, at, size, "jais2", 5)) { return true; }
            if (tf_same_text(data, at, size, "nemotron", 8)) { return true; }
            if (tf_same_text(data, at, size, "nemotron_h", 10)) { return true; }
            if (tf_same_text(data, at, size, "nemotron_h_moe", 14)) { return true; }
            if (tf_same_text(data, at, size, "exaone", 6)) { return true; }
            if (tf_same_text(data, at, size, "exaone4", 7)) { return true; }
            if (tf_same_text(data, at, size, "exaone-moe", 10)) { return true; }
            if (tf_same_text(data, at, size, "rwkv6", 5)) { return true; }
            if (tf_same_text(data, at, size, "rwkv6qwen2", 10)) { return true; }
            if (tf_same_text(data, at, size, "rwkv7", 5)) { return true; }
            if (tf_same_text(data, at, size, "arwkv7", 6)) { return true; }
            if (tf_same_text(data, at, size, "granite", 7)) { return true; }
            if (tf_same_text(data, at, size, "granitemoe", 10)) { return true; }
            if (tf_same_text(data, at, size, "granitehybrid", 13)) { return true; }
            if (tf_same_text(data, at, size, "graniteswitch", 13)) { return true; }
            if (tf_same_text(data, at, size, "granite_swa", 11)) { return true; }
            if (tf_same_text(data, at, size, "chameleon", 9)) { return true; }
            if (tf_same_text(data, at, size, "wavtokenizer-dec", 16)) { return true; }
            if (tf_same_text(data, at, size, "plm", 3)) { return true; }
            if (tf_same_text(data, at, size, "bailingmoe", 10)) { return true; }
            if (tf_same_text(data, at, size, "bailingmoe2", 11)) { return true; }
            if (tf_same_text(data, at, size, "bailingmoe3", 11)) { return true; }
            if (tf_same_text(data, at, size, "dots1", 5)) { return true; }
            if (tf_same_text(data, at, size, "dots3note", 9)) { return true; }
            if (tf_same_text(data, at, size, "arcee", 5)) { return true; }
            if (tf_same_text(data, at, size, "afmoe", 5)) { return true; }
            if (tf_same_text(data, at, size, "laguna", 6)) { return true; }
            if (tf_same_text(data, at, size, "ernie4_5", 8)) { return true; }
            if (tf_same_text(data, at, size, "ernie4_5-moe", 12)) { return true; }
            if (tf_same_text(data, at, size, "hunyuan-moe", 11)) { return true; }
            if (tf_same_text(data, at, size, "hunyuan-dense", 13)) { return true; }
            if (tf_same_text(data, at, size, "hunyuan_vl", 10)) { return true; }
            if (tf_same_text(data, at, size, "hy_v3", 5)) { return true; }
            if (tf_same_text(data, at, size, "smollm3", 7)) { return true; }
            if (tf_same_text(data, at, size, "gpt-oss", 7)) { return true; }
            if (tf_same_text(data, at, size, "lfm2", 4)) { return true; }
            if (tf_same_text(data, at, size, "lfm2moe", 7)) { return true; }
            if (tf_same_text(data, at, size, "dream", 5)) { return true; }
            if (tf_same_text(data, at, size, "smallthinker", 12)) { return true; }
            if (tf_same_text(data, at, size, "llada", 5)) { return true; }
            if (tf_same_text(data, at, size, "llada-moe", 9)) { return true; }
            if (tf_same_text(data, at, size, "seed_oss", 8)) { return true; }
            if (tf_same_text(data, at, size, "grovemoe", 8)) { return true; }
            if (tf_same_text(data, at, size, "apertus", 7)) { return true; }
            if (tf_same_text(data, at, size, "minimax-01", 10)) { return true; }
            if (tf_same_text(data, at, size, "minimax-m2", 10)) { return true; }
            if (tf_same_text(data, at, size, "minimax-m3", 10)) { return true; }
            if (tf_same_text(data, at, size, "cogvlm", 6)) { return true; }
            if (tf_same_text(data, at, size, "rnd1", 4)) { return true; }
            if (tf_same_text(data, at, size, "pangu-embedded", 14)) { return true; }
            if (tf_same_text(data, at, size, "mistral3", 8)) { return true; }
            if (tf_same_text(data, at, size, "eagle3", 6)) { return true; }
            if (tf_same_text(data, at, size, "dflash", 6)) { return true; }
            if (tf_same_text(data, at, size, "mistral4", 8)) { return true; }
            if (tf_same_text(data, at, size, "paddleocr", 9)) { return true; }
            if (tf_same_text(data, at, size, "mimo2", 5)) { return true; }
            if (tf_same_text(data, at, size, "step35", 6)) { return true; }
            if (tf_same_text(data, at, size, "llama-embed", 11)) { return true; }
            if (tf_same_text(data, at, size, "maincoder", 9)) { return true; }
            if (tf_same_text(data, at, size, "kimi-linear", 11)) { return true; }
            if (tf_same_text(data, at, size, "kimi-k3", 7)) { return true; }
            if (tf_same_text(data, at, size, "talkie", 6)) { return true; }
            if (tf_same_text(data, at, size, "mellum", 6)) { return true; }
            if (tf_same_text(data, at, size, "nanbeige", 8)) { return true; }
            if (tf_same_text(data, at, size, "qwen3tts", 8)) { return true; }
            if (tf_same_text(data, at, size, "pockettts", 9)) { return true; }
            if (tf_same_text(data, at, size, "(unknown)", 9)) { return true; }
            return false;
        }
        if (runtime == 3) {
            if (tf_same_text(data, at, size, "clip", 4)) { return true; }
            if (tf_same_text(data, at, size, "llama", 5)) { return true; }
            if (tf_same_text(data, at, size, "llama4", 6)) { return true; }
            if (tf_same_text(data, at, size, "deci", 4)) { return true; }
            if (tf_same_text(data, at, size, "falcon", 6)) { return true; }
            if (tf_same_text(data, at, size, "grok", 4)) { return true; }
            if (tf_same_text(data, at, size, "gpt2", 4)) { return true; }
            if (tf_same_text(data, at, size, "gptj", 4)) { return true; }
            if (tf_same_text(data, at, size, "gptneox", 7)) { return true; }
            if (tf_same_text(data, at, size, "mpt", 3)) { return true; }
            if (tf_same_text(data, at, size, "baichuan", 8)) { return true; }
            if (tf_same_text(data, at, size, "starcoder", 9)) { return true; }
            if (tf_same_text(data, at, size, "refact", 6)) { return true; }
            if (tf_same_text(data, at, size, "bert", 4)) { return true; }
            if (tf_same_text(data, at, size, "modern-bert", 11)) { return true; }
            if (tf_same_text(data, at, size, "nomic-bert", 10)) { return true; }
            if (tf_same_text(data, at, size, "nomic-bert-moe", 14)) { return true; }
            if (tf_same_text(data, at, size, "neo-bert", 8)) { return true; }
            if (tf_same_text(data, at, size, "jina-bert-v2", 12)) { return true; }
            if (tf_same_text(data, at, size, "jina-bert-v3", 12)) { return true; }
            if (tf_same_text(data, at, size, "eurobert", 8)) { return true; }
            if (tf_same_text(data, at, size, "bloom", 5)) { return true; }
            if (tf_same_text(data, at, size, "stablelm", 8)) { return true; }
            if (tf_same_text(data, at, size, "qwen", 4)) { return true; }
            if (tf_same_text(data, at, size, "qwen2", 5)) { return true; }
            if (tf_same_text(data, at, size, "qwen2moe", 8)) { return true; }
            if (tf_same_text(data, at, size, "qwen2vl", 7)) { return true; }
            if (tf_same_text(data, at, size, "qwen3", 5)) { return true; }
            if (tf_same_text(data, at, size, "qwen3moe", 8)) { return true; }
            if (tf_same_text(data, at, size, "qwen3next", 9)) { return true; }
            if (tf_same_text(data, at, size, "qwen3vl", 7)) { return true; }
            if (tf_same_text(data, at, size, "qwen3vlmoe", 10)) { return true; }
            if (tf_same_text(data, at, size, "qwen35", 6)) { return true; }
            if (tf_same_text(data, at, size, "qwen35moe", 9)) { return true; }
            if (tf_same_text(data, at, size, "phi2", 4)) { return true; }
            if (tf_same_text(data, at, size, "phi3", 4)) { return true; }
            if (tf_same_text(data, at, size, "phimoe", 6)) { return true; }
            if (tf_same_text(data, at, size, "plamo", 5)) { return true; }
            if (tf_same_text(data, at, size, "plamo2", 6)) { return true; }
            if (tf_same_text(data, at, size, "plamo3", 6)) { return true; }
            if (tf_same_text(data, at, size, "codeshell", 9)) { return true; }
            if (tf_same_text(data, at, size, "orion", 5)) { return true; }
            if (tf_same_text(data, at, size, "internlm2", 9)) { return true; }
            if (tf_same_text(data, at, size, "minicpm", 7)) { return true; }
            if (tf_same_text(data, at, size, "minicpm3", 8)) { return true; }
            if (tf_same_text(data, at, size, "gemma", 5)) { return true; }
            if (tf_same_text(data, at, size, "gemma2", 6)) { return true; }
            if (tf_same_text(data, at, size, "gemma3", 6)) { return true; }
            if (tf_same_text(data, at, size, "gemma3n", 7)) { return true; }
            if (tf_same_text(data, at, size, "gemma4", 6)) { return true; }
            if (tf_same_text(data, at, size, "gemma4-assistant", 16)) { return true; }
            if (tf_same_text(data, at, size, "gemma-embedding", 15)) { return true; }
            if (tf_same_text(data, at, size, "starcoder2", 10)) { return true; }
            if (tf_same_text(data, at, size, "mamba", 5)) { return true; }
            if (tf_same_text(data, at, size, "mamba2", 6)) { return true; }
            if (tf_same_text(data, at, size, "jamba", 5)) { return true; }
            if (tf_same_text(data, at, size, "falcon-h1", 9)) { return true; }
            if (tf_same_text(data, at, size, "xverse", 6)) { return true; }
            if (tf_same_text(data, at, size, "command-r", 9)) { return true; }
            if (tf_same_text(data, at, size, "cohere2", 7)) { return true; }
            if (tf_same_text(data, at, size, "cohere2moe", 10)) { return true; }
            if (tf_same_text(data, at, size, "dbrx", 4)) { return true; }
            if (tf_same_text(data, at, size, "olmo", 4)) { return true; }
            if (tf_same_text(data, at, size, "olmo2", 5)) { return true; }
            if (tf_same_text(data, at, size, "olmoe", 5)) { return true; }
            if (tf_same_text(data, at, size, "openelm", 7)) { return true; }
            if (tf_same_text(data, at, size, "arctic", 6)) { return true; }
            if (tf_same_text(data, at, size, "deepseek", 8)) { return true; }
            if (tf_same_text(data, at, size, "deepseek2", 9)) { return true; }
            if (tf_same_text(data, at, size, "deepseek2-ocr", 13)) { return true; }
            if (tf_same_text(data, at, size, "deepseek32", 10)) { return true; }
            if (tf_same_text(data, at, size, "deepseek4", 9)) { return true; }
            if (tf_same_text(data, at, size, "chatglm", 7)) { return true; }
            if (tf_same_text(data, at, size, "glm4", 4)) { return true; }
            if (tf_same_text(data, at, size, "glm4moe", 7)) { return true; }
            if (tf_same_text(data, at, size, "glm-dsa", 7)) { return true; }
            if (tf_same_text(data, at, size, "bitnet", 6)) { return true; }
            if (tf_same_text(data, at, size, "bitnet-b1.58", 12)) { return true; }
            if (tf_same_text(data, at, size, "t5", 2)) { return true; }
            if (tf_same_text(data, at, size, "t5encoder", 9)) { return true; }
            if (tf_same_text(data, at, size, "jais", 4)) { return true; }
            if (tf_same_text(data, at, size, "jais2", 5)) { return true; }
            if (tf_same_text(data, at, size, "nemotron", 8)) { return true; }
            if (tf_same_text(data, at, size, "nemotron_h", 10)) { return true; }
            if (tf_same_text(data, at, size, "nemotron_h_moe", 14)) { return true; }
            if (tf_same_text(data, at, size, "exaone", 6)) { return true; }
            if (tf_same_text(data, at, size, "exaone4", 7)) { return true; }
            if (tf_same_text(data, at, size, "exaone-moe", 10)) { return true; }
            if (tf_same_text(data, at, size, "rwkv6", 5)) { return true; }
            if (tf_same_text(data, at, size, "rwkv6qwen2", 10)) { return true; }
            if (tf_same_text(data, at, size, "rwkv7", 5)) { return true; }
            if (tf_same_text(data, at, size, "arwkv7", 6)) { return true; }
            if (tf_same_text(data, at, size, "granite", 7)) { return true; }
            if (tf_same_text(data, at, size, "granitemoe", 10)) { return true; }
            if (tf_same_text(data, at, size, "granitehybrid", 13)) { return true; }
            if (tf_same_text(data, at, size, "chameleon", 9)) { return true; }
            if (tf_same_text(data, at, size, "wavtokenizer-dec", 16)) { return true; }
            if (tf_same_text(data, at, size, "plm", 3)) { return true; }
            if (tf_same_text(data, at, size, "bailingmoe", 10)) { return true; }
            if (tf_same_text(data, at, size, "bailingmoe2", 11)) { return true; }
            if (tf_same_text(data, at, size, "dots1", 5)) { return true; }
            if (tf_same_text(data, at, size, "arcee", 5)) { return true; }
            if (tf_same_text(data, at, size, "afmoe", 5)) { return true; }
            if (tf_same_text(data, at, size, "ernie4_5", 8)) { return true; }
            if (tf_same_text(data, at, size, "ernie4_5-moe", 12)) { return true; }
            if (tf_same_text(data, at, size, "hunyuan-moe", 11)) { return true; }
            if (tf_same_text(data, at, size, "hunyuan-dense", 13)) { return true; }
            if (tf_same_text(data, at, size, "hunyuan_vl", 10)) { return true; }
            if (tf_same_text(data, at, size, "smollm3", 7)) { return true; }
            if (tf_same_text(data, at, size, "gpt-oss", 7)) { return true; }
            if (tf_same_text(data, at, size, "lfm2", 4)) { return true; }
            if (tf_same_text(data, at, size, "lfm2moe", 7)) { return true; }
            if (tf_same_text(data, at, size, "dream", 5)) { return true; }
            if (tf_same_text(data, at, size, "smallthinker", 12)) { return true; }
            if (tf_same_text(data, at, size, "llada", 5)) { return true; }
            if (tf_same_text(data, at, size, "llada-moe", 9)) { return true; }
            if (tf_same_text(data, at, size, "seed_oss", 8)) { return true; }
            if (tf_same_text(data, at, size, "grovemoe", 8)) { return true; }
            if (tf_same_text(data, at, size, "apertus", 7)) { return true; }
            if (tf_same_text(data, at, size, "minimax-m2", 10)) { return true; }
            if (tf_same_text(data, at, size, "cogvlm", 6)) { return true; }
            if (tf_same_text(data, at, size, "rnd1", 4)) { return true; }
            if (tf_same_text(data, at, size, "pangu-embedded", 14)) { return true; }
            if (tf_same_text(data, at, size, "mistral3", 8)) { return true; }
            if (tf_same_text(data, at, size, "eagle3", 6)) { return true; }
            if (tf_same_text(data, at, size, "dflash", 6)) { return true; }
            if (tf_same_text(data, at, size, "mistral4", 8)) { return true; }
            if (tf_same_text(data, at, size, "paddleocr", 9)) { return true; }
            if (tf_same_text(data, at, size, "mimo2", 5)) { return true; }
            if (tf_same_text(data, at, size, "step35", 6)) { return true; }
            if (tf_same_text(data, at, size, "llama-embed", 11)) { return true; }
            if (tf_same_text(data, at, size, "maincoder", 9)) { return true; }
            if (tf_same_text(data, at, size, "kimi-linear", 11)) { return true; }
            if (tf_same_text(data, at, size, "talkie", 6)) { return true; }
            if (tf_same_text(data, at, size, "mellum", 6)) { return true; }
            if (tf_same_text(data, at, size, "(unknown)", 9)) { return true; }
            return false;
        }
        return false;
    }
}
